using System;

namespace SudokuLib.Internal;

internal class SudokuSolver
{
    public bool IsSolved(Sudoku sudoku)
    {
        if (sudoku == null)
            throw new InvalidOperationException();
        
        throw new NotImplementedException();
    }
}